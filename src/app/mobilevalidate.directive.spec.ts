import { MobilevalidateDirective } from './mobilevalidate.directive';

describe('MobilevalidateDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilevalidateDirective();
    expect(directive).toBeTruthy();
  });
});
